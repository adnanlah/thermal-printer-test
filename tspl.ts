import TSPL from 'js-tspl';

// const tspl = new TSPL();
const t = new TSPL();

// tspl.addText('Print test!');

// const cmd = toBytes(tspl.toPrint());

// console.log('================cmd ', {tp: tspl.toPrint()})y